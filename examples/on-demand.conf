# System-wide, local-first automatic forwarding. This file is read by the
# privileged eBPF loader but the SSH process runs as your normal user.
ssh_destination=development-server
remote_host=127.0.0.1
ports=3000-3999,5173
proxy_port=46173
